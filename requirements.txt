# Python dependencies for Spark Job Definition
# These are installed via Fabric Environment (environment.yml)
# This file is for reference and local development only

# Core dependencies (pre-installed in Fabric Spark)
# pyspark>=3.4.0

# Data manipulation
pandas>=2.0.0

# Time-series modeling
statsmodels>=0.14.0

# Model persistence
joblib>=1.3.0

# MLflow (pre-installed in Fabric)
# mlflow>=2.8.0

# Development dependencies (not needed in Fabric)
# black>=23.0.0
# flake8>=6.0.0
# mypy>=1.5.0
# pytest>=7.4.0
